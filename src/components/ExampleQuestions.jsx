/**
 * ExampleQuestions Component
 *
 * 3 clickable starter question chips:
 * - "What is the expense ratio of HDFC Large Cap Fund?"
 * - "What is the exit load for HDFC Mid-Cap Fund?"
 * - "What is the minimum SIP amount for HDFC Small Cap Fund?"
 *
 * Clicking a chip auto-fills and sends the query.
 * Hidden after first message is sent.
 *
 * @see docs/implementationPlan.md - Phase 6, Task 6.5
 */

"use client";

export default function ExampleQuestions({ onSelect }) {
  return (
    <div id="example-questions">
      <p>ExampleQuestions — Coming in Phase 6</p>
    </div>
  );
}
