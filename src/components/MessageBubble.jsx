/**
 * MessageBubble Component
 *
 * Renders individual chat messages with distinct styling:
 * - User bubble: right-aligned, accent background
 * - Bot bubble: left-aligned, surface background with citation + date
 * - Refusal bubble: amber border styling
 *
 * @see docs/implementationPlan.md - Phase 6, Task 6.6
 */

"use client";

export default function MessageBubble({ message }) {
  return (
    <div id="message-bubble">
      <p>MessageBubble — Coming in Phase 6</p>
    </div>
  );
}
