/**
 * ChatWindow Component
 *
 * Scrollable message area that displays the conversation thread.
 * Shows welcome message on first load and auto-scrolls to latest message.
 * Displays ExampleQuestions as empty state.
 *
 * @see docs/implementationPlan.md - Phase 6, Task 6.7
 */

"use client";

export default function ChatWindow({ messages, isLoading }) {
  return (
    <div id="chat-window">
      <p>ChatWindow — Coming in Phase 6</p>
    </div>
  );
}
