/**
 * InputBar Component
 *
 * Query input with send button.
 * - Send on Enter key
 * - Disabled while waiting for response
 * - Placeholder: "Ask a factual question about HDFC mutual funds..."
 *
 * @see docs/implementationPlan.md - Phase 6, Task 6.8
 */

"use client";

export default function InputBar({ onSend, isLoading }) {
  return (
    <div id="input-bar">
      <p>InputBar — Coming in Phase 6</p>
    </div>
  );
}
